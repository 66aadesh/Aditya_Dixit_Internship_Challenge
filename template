<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Output</title>
    <style>
        body {
            background-color: white;
            color: black;
            font-family: 'Times New Roman', Times, serif;
        }
    </style>
</head>
<body>
    <h1>Output</h1>
    <table border="1">
        <tr>
            <th>Date</th>
            <th>Pick Activities</th>
            <th>Place Activities</th>
            <th>Inside Duration</th>
            <th>Outside Duration</th>
        </tr>
        {% for i in range(dates|length) %}
            <tr>
                <td>{{ dates[i] }}</td>
                <td>{{ pick[i] }}</td>
                <td>{{ place[i] }}</td>
                <td>{{ inside_duration[i] }}</td>
                <td>{{ outside_duration[i] }}</td>
            </tr>
        {% endfor %}
    </table>
</body>
</html>
